<Page xmlns="http://schemas.nativescript.org/tns.xsd"
  navigatingTo="pageNavigatingTo" actionBarHidden="true">

  <GridLayout rows="auto, *" swipe="backSwipe">
    <!-- HEADER -->
    <GridLayout columns="auto, *" rows="auto, auto" class="page-header">
      <GridLayout class="action-bar" colSpan="2">
        <StackLayout tap="backTap" orientation="horizontal" class="action-wrap" horizontalAlignment="left">
          <Image stretch="none" class="icon-image" src="res://ic_arrow_back"/>
          <Label text="Back" class="back-text" />
        </StackLayout>
       <Label text="Wedstrijd details" class="action-bar-title" />

        <StackLayout orientation="horizontal" horizontalAlignment="right">
          <!-- ToDo: Make here a link to the team page when it is a verslag -->
          <GridLayout class="action-wrap" tap="showMapTap" columns="auto" horizontalAlignment="right">
            <Label class="icon-material" text="&#xf332;"/>
          </GridLayout>

          <GridLayout class="action-wrap" tap="shareTap" columns="auto" horizontalAlignment="right">
            <Image stretch="none" class="icon-image" src="res://ic_share"/>
          </GridLayout>
        </StackLayout>
      </GridLayout>

    </GridLayout>

    <!-- Content -->
    <ScrollView row="1" id="scroll">
    
      <StackLayout>
      
        <StackLayout class="content-container" >
            <Label text="Wedstrijd detail pagina" class="page-title" />
            
            <Label text="nou ja, wedstijd details zoals:" /> 
            
            <Label text="{{WedNr}}" /> 
            <Label text="{{Type}}" /> 
            <Label text="{{Datum}}" /> 
            <Label text="{{Tijd}}" />
            <Label text="{{Thuis}}" /> 
            <Label text="{{Uit}}" />  
            
            <!-- Line -->
            <StackLayout class="horizontal-line" margin="0 0 0 0"/>   
            
            <Label text="Hier komen ook" /> 
            <Label text="- routebeschrijving" />            
            <Label text="- voeg toe aan agenda" />            
            <Label text="- kaartje" />            
            <Label text="- club logo's" />            
                      
            <Button class="button share-button material-icon" tap="shareButtonTap" text="&#xf332; Zet in agenda"/>
            
        <!-- ToDo: Make here a list view met related posts -->
        </StackLayout>
      </StackLayout>
    </ScrollView>
  </GridLayout>
</Page>