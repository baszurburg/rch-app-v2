<Page xmlns="http://schemas.nativescript.org/tns.xsd"
   xmlns:PullRefresh="nativescript-pulltorefresh"
  loaded="pageLoaded" actionBarHidden="true">

  <GridLayout rows="auto, *" swipe="backSwipe">
    <!-- HEADER -->
    <StackLayout class="page-header">
      <GridLayout class="action-bar">
        <StackLayout tap="backTap" orientation="horizontal" class="action-wrap" horizontalAlignment="left">
          <Image stretch="none" class="icon-image" src="res://ic_arrow_back"/>
          <Label text="Back" class="back-text" />
        </StackLayout>
        
        <Label text="Reset wachtwoord" class="action-bar-title" />
      </GridLayout>
    </StackLayout>

    <!-- Content -->
    <ScrollView row="1" id="scroll">
      <StackLayout row="1" class="content-offset">
        <Label text="Voer je email adres in waarmee je de RCH app gebruikt" textWrap="true" />

        <GridLayout>
          <TextField text="{{ user.email }}" id="email" hint="Email Address" keyboardType="email" autocorrect="false" autocapitalizationType="none" returnKeyType="done" returnPress="reset" />
          <Border class="textFieldBorder" />
          <ActivityIndicator busy="{{ authenticating }}" />
        </GridLayout>

        <Button text="Reset password" id="submit-button" tap="reset" />
		  </StackLayout>

    </ScrollView>

  </GridLayout>
</Page>